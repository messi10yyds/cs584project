server:
  port: 8080

spring:
  application:
    name: backend
  datasource:
    url: jdbc:mysql://localhost:3306/diabetes_mvp?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC
    username: root
    password: lyfmessi2001
    driver-class-name: com.mysql.cj.jdbc.Driver

mybatis:
  mapper-locations: classpath:/mapper/*.xml
  type-aliases-package: com.basic.backend.entity
  configuration:
    map-underscore-to-camel-case: true

jwt:
  # HS256 secret 建议 >= 32 字符（否则 jjwt 可能报 key 太短）
  secret: "replace_with_a_very_long_secret_key_at_least_32_chars"
  expire-ms: 86400000